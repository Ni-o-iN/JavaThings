import static pr.MakeItSimple.*;

public class SortMain {

	// So wird das Sortieren von iea aufgerufen
	// Wie SortInterface aussehen muss, m�ssen Sie selbst �berlegen

	private static void sort(Comparable[] iea, SortInterface salg) {
	}

	public static void main(String[] args) {

		UI ui = new UI();
		ui.main(args);
		// code
	}
}